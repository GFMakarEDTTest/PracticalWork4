
&НаКлиенте
Процедура ВывестиСообщение(Команда)
	
	Если НЕ СтрДлина(ТекстСообщения) = 0 Тогда
		ТекстСообщения = "Привет, ГИТ!";
	Иначе     
		ТекстСообщения = СтрШаблон("%1 %2", ТекстСообщения, "Привет, ГИТ!");
	КонецЕсли; 
	
КонецПроцедуры
