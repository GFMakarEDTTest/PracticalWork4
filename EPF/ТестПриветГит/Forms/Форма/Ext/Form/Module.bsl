
&НаКлиенте
Процедура ВывестиСообщение(Команда)
	
	Если СтрДлина(ТекстСообщения) = 0 Тогда
		ТекстСообщения = "Привет, ГИТ!";
	Иначе     
		ТекстСообщения = СтрШаблон("%1 
									|%2", ТекстСообщения, "Привет, ГИТ!");
	КонецЕсли; 
	
КонецПроцедуры
